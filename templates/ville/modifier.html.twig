{% extends 'base.html.twig' %}
{% block title %} Modifier le site | {{ parent() }}{% endblock %}
{% block main %}
    <div class="flash">{{ include('inc/flashmessage.html.twig') }}</div>
    <div class="row" id="profil">
        <div class="col-sm-12"><h2>Modifier la ville</h2></div>
        <div class="col-sm-3"></div>
        <div class="col-sm-6">
            <div class="form-group">
                {{ form_start(form) }}<br>
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
        <div class="col-sm-3"></div>
    </div>
{% endblock %}