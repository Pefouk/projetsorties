{% extends 'base.html.twig' %}
{% block title %} Modifier le site | {{ parent() }}{% endblock %}
{% block main %}
    <div class="row" id="profil">
        <div class="col-sm-12"><h2>Nouvelle ville</h2></div>
        <div class="col-sm-3"></div>
        <div class="col-sm-6">
            <div class="form-group">
                {{ form_start(form) }}<br>
                {{ form_widget(form) }}
                <input type="submit" value="Valider" class="btn btn-primary btn-block"/>
                {{ form_end(form) }}
            </div>
        </div>
        <div class="col-sm-3"></div>
    </div>
{% endblock %}