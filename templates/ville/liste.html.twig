{% extends 'base.html.twig' %}

{% block title %}Liste des ville{% endblock %}

{% block head %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/table.css') }}">
{% endblock %}

{% block main %}
    <div class="flash">{{ include('inc/flashmessage.html.twig') }}</div>
    <div class="sorties">
    <div class="row">
        <div class="col-sm-1"></div>
        <div class="col-sm-10">
            <div class="showSorties">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
                <a href="{{ path('ville_creer') }}" class="btn btn-primary btn-block">Nouvelle ville</a>
                {% if villes is empty %}
                    <h2>Pas de villes trouvés !</h2>
                {% else %}
                    <div class="table-responsive-md">
                        <table class="table table-striped">
                            <thead>
                            <tr>
                                <th>Ville</th>
                                <th>Code postal</th>
                                <th>Actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for ville in villes %}
                                <tr>
                                    <td>{{ ville.nom | upper }}</td>
                                    <td>{{ ville.codePostale | upper }}</td>
                                    <td><a href="{{ path('ville_modifier', {'id': ville.id}) }}">Modifier </a>
                                        -
                                        <a href="{{ path('ville_supprimer', {'id': ville.id}) }}">Supprimer</a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                {% endif %}
            </div>
        </div>
        <div class="col-sm-1"></div>
    </div>
{% endblock %}